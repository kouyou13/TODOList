{
  "name": "todo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix ./frontend\" \"cd backend && python3 main.py\"",
    "fmt": "prettier --write --ignore-path .gitignore './**/*.{js,jsx,ts,tsx,json,css,py}'",
    "fmt:check": "eslint-config-prettier 'src/**/*.{js,jsx,ts,tsx,json,css,py}'"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm run fmt"
    }
  },
  "dependencies": {
    "concurrently": "^9.1.0",
    "prettier": "^3.3.3"
  }
}
